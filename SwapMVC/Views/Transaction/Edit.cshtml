@model SwapMVC.Models.SwapTransaction

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>SwapTransaction</legend>

        @Html.HiddenFor(model => model.ID)

        <div class="editor-label">
            @Html.LabelFor(model => model.BookID, "Book")
        </div>
        <div class="editor-field">
            @Html.DropDownList("BookID", String.Empty)
            @Html.ValidationMessageFor(model => model.BookID)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.SwapItemID, "SwapItem")
        </div>
        <div class="editor-field">
            @Html.DropDownList("SwapItemID", String.Empty)
            @Html.ValidationMessageFor(model => model.SwapItemID)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.SwapDate)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.SwapDate)
            @Html.ValidationMessageFor(model => model.SwapDate)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
